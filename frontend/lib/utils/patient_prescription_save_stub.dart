import 'dart:typed_data';

Future<String> savePatientPrescriptionPdf(Uint8List bytes, String filename) async {
  throw UnsupportedError(
    'Enregistrement non disponible sur cette plateforme.',
  );
}
